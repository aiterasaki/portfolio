<div class="container" >
    <!-- 任意のID指定。クラスとデータ属性の指定。 -->
  <div id="carousel-example" class="carousel slide" data-ride="carousel">
    <!-- インジケーターの設置。下部の○●ボタン。 -->
    <ol class="carousel-indicators">
      <li data-target="#carousel-example" data-slide-to="0" class="active"></li>
      <li data-target="#carousel-example" data-slide-to="1"></li>
      <li data-target="#carousel-example" data-slide-to="2"></li>
      <li data-target="#carousel-example" data-slide-to="3"></li>
    </ol>

    <!-- スライドの内容 -->
    <div class="carousel-inner">
      <div class="item active">
  			<%= image_tag '/images/smile.jpg', :size => "940x390" %>
  		<div class="carousel-caption">
  			<h1>スマホでかんたん</h1>
  			<p>誰でも簡単に、売ったり買ったりを楽しめる</p>
  			<button type="button" class="btn btn-primary">ページ詳細</button>
  		</div>
      </div>

      <div class="item">
  		<%= image_tag '/images/mens.jpg', :size => '940x390' %>
  		<div class="carousel-caption">
  			<h1>簡単に出品できる</h1>
  			<p>スマホで写真を撮って特徴を入力するだけで、簡単に出品できます。</p>
  			<button type="button" class="btn btn-primary">ページ詳細</button>
  		</div>
      </div>
      <div class="item">
  		<%= image_tag '/images/hand_made.jpg', :size => '940x390' %>
  		<div class="carousel-caption">
  			<h1>すぐに購入できる</h1>
  			<p>ボタン1つで即購入。あとは出品者からの発送を待つだけです。</p>
  			<button type="button" class="btn btn-primary">ページ詳細</button>
  		</div>
      </div>
      <div class="item">
  		<%= image_tag '/images/ladies.jpg', :size => '940x390' %>
  		<div class="carousel-caption">
  			<h1>商品がたくさん</h1>
  			<p>毎日数十万品以上の新商品が出品されるので、ほしいものがきっと見つかります。</p>
  			<button type="button" class="btn btn-primary">ページ詳細</button>
  		</div>
      </div>
    </div>

    <!-- 左右の移動ボタン -->
    <a class="left carousel-control" href="#carousel-example" data-slide="prev">
      <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
    </a>
    <a class="right carousel-control" href="#carousel-example" data-slide="next">
      <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
    </a>
  </div>
</div>

_________________

<div> 
<%= form_tag(root_path, method: "get") do %>

<%= label_tag :name, "名前" %>
<%= text_field_tag :name, params[:name] %>

<%= label_tag :brand, "ブランド名" %>
<%= text_field_tag :brand, params[:brand] %>

<%= label_tag :status, "商品の状態" %>
<%= select_tag :status, options_for_select(Product.statuses), include_blank: true %>

<%= label_tag :burden_of_shipping_fee, "配送料の負担" %>
<%= select_tag :burden_of_shipping_fee, options_for_select(Product.burden_of_shipping_fees), include_blank: true %>

<%= label_tag :sell_flg, "販売状況" %>
<%#= select_tag :sell_flg, options_for_select([["すべて", ], ["販売中", true], ["売り切れ", false]], :sell_flg) %>


<%#= collection_select :sell_flg, options_for_select(Product.sell_flgs), include_blank: true %>

<%= submit_tag "検索" %>
<% end %>
</div>


__________________
<!-- Page Content -->
    <div class="container">
      <div class="row">
        <div class="col-md-2">
          <h1 class="my-4">Shop Name
          </h1>
          <div class="list-group">
            <a href="#" class="list-group-item">レディース
            </a>

            <% @ladies_categories.each do | ladies_category| %>
              <p><%= link_to ladies_category.name, root_path(ladies_category_id: ladies_category.id) %></p>
            <% end %>

            <a href="#" class="list-group-item">メンズ</a>

            <% @mens_categories.each do |mens_category| %>
              <p><%= link_to mens_category.name,root_path(mens_category_id: mens_category.id) %></p>
            <% end %>

            <a href="#" class="list-group-item">ハンドメイド</a>

            <% @handmade_categories.each do |handmade_category|%>
            <p><%= link_to handmade_category.name,root_path(handmade_category_id:handmade_category.id) %></p>
            <% end %>
          </div>
        </div>
        <!-- /.col-md-2 -->
        <div class="col-md-10">
          <!-- </div> -->
          <div class="row">
      			<% @products.each do |product| %>
      				<div class="col-md-4 item">  				
                <p>
                  <% if product.sell_flg == false %>
                    <div class="sold_out_back">
                      <div class="sold_out">SOLD                
                      </div>
                    </div>
                      <td><%= attachment_image_tag product, :thumbnail, :fill, 220, 220, format: 'jpeg'  %>
                      </td>
                  <% else %>
                    <div class="blank">
                      <div class="blank">    
                      </div>
                    </div>
                      <td><%= attachment_image_tag product, :thumbnail, :fill, 220, 220, format: 'jpeg' %>
                      </td>
                  <% end %>
                </p>
        					<!-- 商品名 -->
        					<span><%=link_to product.name, product_path(product.id) %>     
                  </span>
        					<br>
        					<span>¥<%=link_to product.price, product_path(product.id) %>(税込)
                  </span>
      				</div>
      			<% end %>
          </div>
          <!-- /.row -->
        </div>
        <!-- /.col-md-10 -->
      </div>
      <!-- /.row -->
    </div>
    <!-- /.container